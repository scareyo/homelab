---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
  namespace: authentik
spec:
  parentRefs:
    - name: internal
      namespace: gateway
  hostnames:
    - sonarr.apps.int.scarey.me
  rules:
    - backendRefs:
        - name: ak-outpost-authentik-embedded-outpost
          port: 9000
