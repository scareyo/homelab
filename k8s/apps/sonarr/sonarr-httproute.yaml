---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
spec:
  parentRefs:
    - name: internal
      namespace: gateway
  hostnames:
    - sonarr.vegapunk.cloud
  rules:
    - backendRefs:
        - name: oauth2-proxy
          port: 80
