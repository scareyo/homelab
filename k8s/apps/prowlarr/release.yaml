---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prowlarr
spec:
  chart:
    spec:
      chart: prowlarr
      version: 1.49.0
      sourceRef:
        kind: HelmRepository
        name: pree
  releaseName: prowlarr
  interval: 10m
  values:
    env:
      PROWLARR__AUTH__METHOD: External
