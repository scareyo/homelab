---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-operator
spec:
  chart:
    spec:
      chart: flux-operator
      version: 0.28.0
      sourceRef:
        kind: HelmRepository
        name: flux-operator
  releaseName: flux-operator
  interval: 10m
