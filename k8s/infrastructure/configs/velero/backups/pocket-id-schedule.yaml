---
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: pocket-id
  namespace: velero
spec:
  schedule: 0 4 * * *
  template:
    includedNamespaces:
      - pocket-id
    includedResources:
      - pv
      - pvc
    snapshotVolumes: true
    snapshotMoveData: true
