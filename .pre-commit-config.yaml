repos:
  - repo: local
    hooks:
      - id: generate-manifests
        name: Generate manifests
        entry: ./generate-manifests.sh
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-commit]
