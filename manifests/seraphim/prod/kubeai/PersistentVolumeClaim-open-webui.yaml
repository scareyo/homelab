apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/component: open-webui
    app.kubernetes.io/instance: kubeai
  name: open-webui
  namespace: kubeai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
