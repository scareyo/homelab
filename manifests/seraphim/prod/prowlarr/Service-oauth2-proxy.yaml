apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: prowlarr
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: oauth2-proxy
