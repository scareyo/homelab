apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  labels:
    app: gitlab-shell
    chart: gitlab-shell-9.8.2
    heritage: Helm
    release: gitlab
  name: gitlab-gitlab-shell
  namespace: gitlab
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: gitlab-shell
      release: gitlab
