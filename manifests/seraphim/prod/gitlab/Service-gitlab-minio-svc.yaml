apiVersion: v1
kind: Service
metadata:
  labels:
    app: minio
    chart: minio-0.4.3
    heritage: Helm
    release: gitlab
  name: gitlab-minio-svc
  namespace: gitlab
spec:
  ports:
    - name: http
      port: 9000
      protocol: TCP
      targetPort: 9000
  selector:
    app: minio
    component: app
    release: gitlab
  type: ClusterIP
