apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  labels:
    app: gitaly
    chart: gitaly-9.8.2
    heritage: Helm
    release: gitlab
  name: gitlab-gitaly
  namespace: gitlab
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: gitaly
      release: gitlab
