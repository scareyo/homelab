apiVersion: v1
kind: Service
metadata:
  labels:
    app: registry
    chart: registry-0.7.0
    heritage: Helm
    release: gitlab
  name: gitlab-registry
  namespace: gitlab
spec:
  ports:
    - name: http-registry
      port: 5000
      protocol: TCP
      targetPort: http
  selector:
    app: registry
    release: gitlab
  type: ClusterIP
