apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  labels:
    app: kas
    chart: kas-9.8.2
    heritage: Helm
    release: gitlab
  name: gitlab-kas
  namespace: gitlab
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: kas
      release: gitlab
