apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: garage
  namespace: velero
spec:
  data:
    - remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: /velero/GARAGE_KEY
        metadataPolicy: None
      secretKey: key
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target: {}
