---
- name: Configure seraphim cluster
  hosts: bootstrap
  connection: local
  tasks:
    - name: Wait for healthy cluster
      ansible.builtin.command: "talosctl --nodes {{ ansible_host }} --endpoints {{ ansible_host }} health"
      changed_when: true

    - name: Download admin kubeconfig
      ansible.builtin.command: >
        talosctl --nodes {{ ansible_host }} --endpoints {{ ansible_host }} kubeconfig {{ inventory_dir }}/../.kube/config --force
      changed_when: true

    - name: Change kubeconfig permissions
      ansible.builtin.file:
        path: "{{ inventory_dir }}/../.kube/config"
        mode: '0600'
    
    - name: Deploy ArgoCD
      kubernetes.core.helm:
        name: argocd
        chart_ref: "../../apps/argocd"
        dependency_update: true
        release_namespace: argocd
        create_namespace: true

    - name: Deploy ArgoCD apps
      kubernetes.core.helm:
        name: argocd-apps
        chart_ref: "../../apps/argocd-apps"
        release_namespace: argocd
        create_namespace: true

        #    - name: Install ArgoCD
        #      kubernetes.core.k8s:
        #        namespace: argocd
        #        definition: >
        #          {{ lookup('ansible.builtin.url', 'https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml', split_lines=False)
        #            | from_yaml_all | list }}
        #        state: present
        #
        #    - name: Create MetalLB application
        #      kubernetes.core.k8s:
        #        name: metallb
        #        namespace: argocd
        #        api_version: argoproj.io/v1alpha1
        #        kind: Application
        #        definition:
        #          spec:
        #            project: "default"
        #            source:
        #              repoURL: "https://github.com/scareyo/homelab.git"
        #              targetRevision: develop-talos
        #              path: 'apps/metallb'
        #            destination:
        #              server: https://kubernetes.default.svc
        #              namespace: 'metallb'
        #            syncPolicy:
        #              automated:
        #                prune: true
        #              managedNamespaceMetadata:
        #                labels:
        #                  pod-security.kubernetes.io/enforce: privileged
        #                  pod-security.kubernetes.io/audit: privileged
        #                  pod-security.kubernetes.io/warn: privileged
        #              syncOptions:
        #                - CreateNamespace=true
        #
        #    - name: Create ArgoCD ApplicationSet
        #      kubernetes.core.k8s:
        #        name: apps
        #        namespace: argocd
        #        api_version: argoproj.io/v1alpha1
        #        kind: ApplicationSet
        #        definition:
        #          spec:
        #            goTemplate: true
        #            goTemplateOptions: ["missingkey=error"]
        #            generators:
        #              - git:
        #                  repoURL: "https://github.com/scareyo/homelab.git"
        #                  revision: develop-talos
        #                  directories:
        #                    - path: apps/*
        #                    - path: apps/metallb
        #                      exclude: true
        #            template:
        #              metadata:
        #                name: '{% raw %}{{.path.basename}}{% endraw %}'
        #              spec:
        #                project: "default"
        #                source:
        #                  repoURL: "https://github.com/scareyo/homelab.git"
        #                  targetRevision: develop-talos
        #                  path: '{% raw %}{{.path.path}}{% endraw %}'
        #                destination:
        #                  server: "https://kubernetes.default.svc"
        #                  namespace: '{% raw %}{{.path.basename}}{% endraw %}'
        #                syncPolicy:
        #                  automated:
        #                    prune: true
        #                  syncOptions:
        #                    - CreateNamespace=true
        #        state: present
