---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: internal
  namespace: gateway
spec:
  gatewayClassName: cilium
  infrastructure:
    annotations:
      io.cilium/lb-ipam-ips: 10.10.20.50
  listeners:
    - protocol: HTTP
      port: 80
      name: http-gateway
      hostname: "*.apps.int.scarey.me"
      allowedRoutes:
        namespaces:
          from: All
